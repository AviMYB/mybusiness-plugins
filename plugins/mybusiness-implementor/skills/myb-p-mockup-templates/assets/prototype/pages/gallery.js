import { catalog, classics } from '../catalog.js';
import { esc, icon } from '../ui.js';
export default function gallery() {
  return `<section class="hero"><div><p class="eyebrow">ספריית מוקאפים אינטראקטיביים</p><h2>ספריית תבניות MyBusiness</h2><p>בחרו תבנית לפתיחת הדגמה מקומית. מבנה מסכי הליבה הושווה למערכת; הפעולות והנתונים מיועדים להתנסות.</p><a href="#records" class="outline">התחילו ברשימת הרשומות</a></div><div class="hero-count">12<small>דפוסי מסך אינטראקטיביים</small></div></section><p class="gallery-note">מסכי ליבה: לקוחות וכרטיס לקוח, מבט על, הגדרות, מכירות ויומן. יתר התבניות מציעות תהליכים נוספים באותה שפה חזותית.</p><div class="grid">${catalog.map(t => `<a class="template-card" href="#${t.id}">${icon(t.icon)}<h3>${t.title}</h3><p>${t.text}</p><div class="meta"><span class="tag">${['records','dashboard','settings','kanban','calendar'].includes(t.id)?'מבנה הושווה למערכת':'תהליך מוצע'}</span><span>לפתיחת ההדגמה ←</span></div></a>`).join('')}</div><section class="card" style="margin-top:24px"><h2>ספריית העיצוב המקורית</h2><p class="muted">14 תבניות HTML סטטיות לפי מודול. משמשות בסיס חזותי למסכים נוספים; ההתנהגות בהדגמות החדשות היא הצעה לתכנון.</p><div class="reference-links">${classics.map(([url,label]) => `<a href="reference-pages/${url}" target="_blank" rel="noopener">${esc(label)} ↗</a>`).join('')}</div></section>`;
}
